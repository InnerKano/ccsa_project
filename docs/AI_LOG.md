# AI Log

Record of AI usage in the project. This file is filled in whenever AI contributes something non-trivial (Yellow/Red zone or architectural decisions), according to the `AI_RULES.md` protocol.

It is the direct input for answering in the video: "where did AI help most? where did it fail?" with real evidence instead of memory.

## Format

```
YYYY-MM-DD | [module] | What was asked | Usefulness (1-5) | What was fixed manually
```

## Entries

| Date | Module | What was asked | Usefulness (1-5) | Manual correction |
|---|---|---|---|---|
| 2026-07-04 | architecture | Create the initial backend/frontend folder structure and decide which placeholder modules to keep | 4 | Kept the scaffold minimal and avoided premature feature-specific files; only the core structure and entrypoint were preserved. |
| 2026-07-04 | backend | Step 2.1 walking skeleton: FastAPI app, GET /health, requirements.txt, Dockerfile | 5 | No corrections needed; health endpoint matches API.md contract, no DB/CORS yet (deferred to Steps 3–4). |
| 2026-07-05 | backend | Add pytest health test + README run/test instructions | 5 | Clarified single test suite for venv and Docker; no duplicate e2e for Step 2.1. |
| 2026-07-05 | infra | Step 3 Docker Compose: backend + frontend + Postgres, core/config + database wiring | 5 | DB check on startup in Compose; SKIP_DB_CHECK for pytest/venv-only; no migrations yet (Step 4). |
| 2026-07-05 | database | Step 4 Alembic wiring: env.py, get_db(), core/models registry; no feature tables | 5 | First migration deferred to auth feature per DATA_MODEL.md; no empty revisions. |
| 2026-07-05 | architecture | Step 5 validate structure: module stubs, test_structure, docs alignment, D11 | 5 | ARCHITECTURE tree updated to match disk; bootstrap complete, next = auth. |
| 2026-07-05 | auth | A1 auth module: User model, bcrypt/JWT, register/login, first migration, integration tests, minimal login/register UI | 4 | Switched from passlib to direct bcrypt (bcrypt 5.x incompatibility); pending Red-zone review before commit. |
| 2026-07-05 | statements | A2 statements module: CSV parse in memory, Statement/Transaction models, migration, per-user CRUD, sample.csv fixture, integration + unit tests | 4 | Pending Red-zone review (per-user isolation, no sensitive data in errors); no frontend upload UI yet (A4). |
| 2026-07-05 | statements | A2.1 ingestion hardening: analyze real bank exports vs idealized CSV, design pluggable `ingest/` pipeline (formats/locales/encoding, EN+ES, US/EU, debit-credit), synthetic ES fixture, D15 | 4 | Kept PDF/statement-dump parsing out of MVP (scope/Risk #3); flagged PII in real fixtures → gitignored, only synthetic samples versioned; refined REQUIREMENTS assumption via D15 (not by rewriting REQUIREMENTS). |
| 2026-07-05 | testing | Fix cross-run test isolation for DB-backed suites | 5 | Root cause: app-level commit() leaked past the outer transaction; fixed with SQLAlchemy `join_transaction_mode="create_savepoint"` in auth + statements conftests. |
| 2026-07-05 | analysis | A3 analysis module (Layer 1, rules-only): recurrence detection, bilingual categorization, savings + recommendations, `analyses`/`detected_subscriptions`/`recommendations` models + migration, per-user isolation, POST/GET endpoints, unit + integration tests | 4 | Defined explainable rule thresholds by hand (≥2 months, 25% amount spread) and a discretionary-only savings heuristic (D16) instead of flagging all recurring charges; kept LLM out (rules-only, D2) leaving a documented Layer 2 seam; pending Red-zone review (per-user scoping on analysis + no sensitive data in errors). |
| 2026-07-05 | frontend | A4.1 foundations: CORS allow-list (`CORS_ORIGINS`), Tailwind v4 design tokens, `components/ui/` primitives, shared `apiFetch` client, `AuthProvider` + SWR, Compose `npm install` on frontend startup | 4 | Red-zone: reviewed CORS config (no `*`, explicit origins). Yellow: reconciled `middle-phases.md` with `ARCHITECTURE.md` frontend stack (Tailwind + SWR + Context, not inline-only MVP). Login/register screens still pre-design-system until A4.2. |
| 2026-07-05 | frontend | A4.2 auth UX: login/register on design system, `GuestOnly`/`RequireAuth`, `AppShell`, landing page, protected `/dashboard` | 4 | Migrated forms to `useAuth()` (no direct `saveToken` in pages); client-side guards only (JWT in localStorage — no middleware). Nav items extended in A4.3+ via `AppShell` `NAV_ITEMS`. |
| 2026-07-05 | frontend | Author `frontend/DESIGN.md` — design system & UX/UI guide (principles, tokens, component/layout catalog, state & a11y conventions, future recommendations) reverse-documented from the A4.1–A4.2 implementation | 4 | Documented only what is actually built (tokens/components verified against source); recommended future items kept as "not yet built" to avoid scope creep; linked from ARCHITECTURE.md, implement-feature.md, components README as the UI source of truth. |
| 2026-07-05 | frontend | A4.3 upload: `lib/api/statements.ts`, `/upload` + `StatementUploadForm` (multipart POST, currency, advanced mapping in `<details>`), `AppShell` nav, dashboard upload success feedback | 4 | Included `listStatements()` in API module for A4.4 reuse; client validates extension only (parse errors from API `detail`); no raw file content in UI/errors. |
| 2026-07-06 | frontend | A4.4 dashboard: `lib/api/analysis.ts`, SWR statement/analysis lists, run/re-run analysis, `StatementCard`/`StatementList`, `/analysis/[id]` headline KPIs | 4 | Latest analysis per statement via map on DESC-ordered list (D10); redirect to `/analysis/{id}` after POST; full subscription UI deferred to A4.5 on same route. |
| 2026-07-06 | frontend | A4.5 results: `AnalysisDetailView`, `SubscriptionList`, `RecommendationList`, currency from `getStatement`, empty states (D16 copy) | 4 | Composable `components/analysis/` per DESIGN.md; closes Phase A browser flow (upload → analyze → view subscriptions + savings + recommendations). |
| 2026-07-06 | docs | A4.6 closeout: align `middle-phases.md`, `implement-feature.md` (A4 sub-phases, SWR pattern, Phase A happy path), `ARCHITECTURE.md` frontend tree, root `README.md`, new `frontend/README.md` | 5 | Docs now reflect how A4 was actually built (not aspirational); `DESIGN.md` remains UI source of truth; Phase A marked complete pending A4.6 commit. |
| 2026-07-06 | analysis | Analyze real US bank/card statements (Capital One, Discover, BoA) vs current parser/analyzer; plan and implement the D17 realism pass on the delimited path: merchant-noise stripping in `canonical_merchant`, expanded EN+ES vocabulary, single-statement subscription signals (bank `RECURRING` marker → `monthly`; known-service single occurrence → `suspected`); synthetic multi-month fixture `caso4`; updated rules tests | 4 | Red zone: real PDFs were untracked and NOT git-ignored (real PII) → added `backend/fixtures/real_samples/` + `*.pdf`/`*.docx` to `.gitignore`, whitelisted only synthetic `samples/*.csv`. Kept new coverage as data (`vocabulary.py`), engine change minimal; introduced a distinct `suspected` cadence to stay honest (one charge ≠ proven cadence). Verified 55 tests green + end-to-end pipeline run on the fixture and real CSV. PDF ingestion deliberately still out (future adapter). |
| 2026-07-06 | statements | D18 PDF ingestion: extract shared `ingest/columns.py`, add `ingest/pdf/` adapter (pdfplumber tables + line profiles for Capital One, Discover, BoA), register before delimited parser, API + upload UI accept `.pdf`, integration tests against local real_samples (git-ignored) | 5 | Verified pdfplumber on PyPI; delimited `can_parse` rejects `%PDF` bytes to avoid mis-detection; PDF uses `skip_invalid_rows` for statement noise; Capital One 360 summary-only PDF fails cleanly; 80 tests green including 4 real-PDF integration tests. No commit — user audits first. |
| 2026-07-07 | frontend | Diagnose mobile-only upload failure (client-side exception on Safari iOS while desktop works). Root cause: frontend called collection routes without a trailing slash (`/api/statements`, `/api/analysis`) → FastAPI 307 redirect that mobile WebKit refuses to follow on a preflighted, credentialed multipart POST. Fix: call the exact trailing-slash routes in `lib/api/statements.ts` + `lib/api/analysis.ts` | 4 | Minimal, targeted fix per AI_RULES (no architecture/CORS change vs the proxy alternative). Confirmed via Render logs (307 with no follow-up 201 on mobile) and by contrast with login (`/api/auth/login`, exact match, works). Item routes (`/{id}`) left unchanged (already exact). Exact uncaught client exception still to be confirmed on-device console. |
| 2026-07-07 | frontend | Residual post-307 symptom: a transient "client-side exception" screen appears right after upload on mobile and self-clears after ~15-30s (matches Render free-tier cold start seen in logs: `Application startup complete`). App had no error boundary, so any transient error showed the raw white crash. Added `app/error.tsx` (graceful, recoverable boundary that logs the real error), `app/global-error.tsx`, and SWR cold-start resilience (`onErrorRetry`: retry network/5xx, skip 4xx) in `providers.tsx` | 3 | Static review found no deterministic throw (components handle empty/NaN/`total<=0`); treated as graceful-degradation + diagnostics per ARCHITECTURE.md. error.tsx `console.error` will surface the exact client message on next on-device repro. Inline styles in global-error.tsx are intentional (cannot use globals.css/design system there). |
| 2026-07-07 | frontend | UX reframe of the mobile upload wait (user insight: the file DOES upload/analyze, it's just slow on mobile — so an error screen is wrong and invites duplicate re-uploads). Rebuilt upload feedback: XHR-based `uploadStatement` with real `upload.onprogress`, two-phase UI in `StatementUploadForm` (determinate "Uploading X%" → indeterminate "Analyzing Ns"), inputs disabled + button replaced (no double-submit), transport-drop (`status 0`) shows an info "may have uploaded — check dashboard" instead of an error, and `error.tsx` auto-reloads once on transient chunk-load failures. Updated `DESIGN.md` §6 (long-running operations pattern) | 4 | Followed DESIGN.md (tokens only, role=progressbar/status, a11y) and implement-feature.md Step 5/8. XHR is the one justified bypass of apiFetch (fetch can't report upload progress) — kept inside lib/api/. Logs real upload+parse duration to console for timing. Root UX cause = mobile latency + server parse time, not a code defect. |
| 2026-07-07 | statements | D19 PDF row profiles: diagnose why two real statements (Capital One 360 checking, PNC Virtual Wallet) fail to parse, then refactor `pdf/lines.py` into a `pdf/profiles/` registry (D18 seam) and add both layouts; move the PII fixtures to `real_samples/`; synthetic + integration tests | 4 | Confirmed the "why" by running the parser (not guessing): 360 has a single date + Debit/Credit + running-balance column; PNC has date/amount/description order with section-driven sign. Found two correctness bugs on real data: global dedupe (D18, for bilingual card reprints) silently dropped PNC's genuine repeated charges → made dedupe per-profile; PNC's 2-column layout interleaves summary text into rows → dropped wrapped tails instead of line-joining. Yellow zone (parsing, no auth/isolation change). Red zone: real PII PDFs moved to git-ignored `real_samples/`, committed tests use synthetic pages. Verified against PNC's own totals (all exact) and 97 tests green. No commit — user audits first. |
| 2026-07-07 | analysis | D20 transaction-type categorization: diagnose why the two newly-parsed PDFs showed almost nothing / only Netflix in analysis; add `transfer`/`cash`/`fees` categories + bilingual keyword rules and fix `canonical_merchant` structural-token stripping | 4 | Confirmed by running Layer 1 (not guessing): parsing was fine — the issue was analysis. Two root causes: (1) vocabulary was merchant-centric but these statements are mostly transaction *types* (Zelle/internal transfers, ATM cash, interest, fees), partly a bilingual-ES gap; (2) `canonical_merchant` kept structural words (`FROM`/`TO`/`ZELLE`/`CARD`/ES articles) → garbage names and collapsed distinct funds into one `TO FONDO DE` group. Kept it data-only (`vocabulary.py`), engine untouched. Honesty (D16/D17): rejected the "repeated small charge → subscription" idea (vending ≠ subscription); Google One surfaced as `suspected`, savings stays discretionary-only. Result: 360 `other` 40→10, PNC 21→16, no over-detection; 100 tests green. No commit — user audits first. |
| 2026-07-07 | auth | D24 password strength policy (NIST-aligned): backend `password_policy.py` (min length + blocklist + no-email-in-password + no single-repeat) wired into `RegisterRequest`/`ResetPasswordRequest`; frontend mirror `lib/auth/passwordPolicy.ts` + `PasswordField` (show/hide) + `PasswordStrengthMeter` + confirm field on register/reset | 4 | Red zone: chose length + blocklist over forced composition per NIST/UX (composition pushes predictable patterns); rejected zxcvbn (~800KB, Risk #3) for a small in-repo blocklist. Kept the server as source of truth and documented the intentional backend/frontend rule duplication. Edge Tools flagged `aria-*` expressions on native elements → routed `PasswordField` through the `Input` primitive (matches the codebase pattern) and used a dynamic `aria-label` instead of `aria-pressed`. No commit — user audits first. |
| 2026-07-07 | auth | D23 password recovery: `forgot-password` + `reset-password` endpoints using a **stateless JWT bound to the user's password hash** (single-use, self-invalidating, no token table/migration); provider-agnostic `shared/email/` (SMTP + dev console fallback, factory by `EMAIL_ENABLED`); frontend `/forgot-password` + `/reset-password` pages, `lib/api/auth.ts` calls, "Forgot password?" link; config `EMAIL_*` + `FRONTEND_URL` + `PASSWORD_RESET_EXPIRE_MINUTES` | 4 | Red zone reviewed line by line: chose hash-binding over a DB token table (leverages JWT as requested, gives single-use + auto-invalidation for free; DB table only wins on audit/explicit revocation, not needed for MVP) and over a plain purpose-only JWT (not single-use). Enforced no-enumeration (identical 200), generic reset errors, force re-login (no auto-login off an email link). Reset verifies by reading the unverified `sub`, loading the user, then validating the signature against their current hash. No new dependency (stdlib smtplib). Flagged rate limiting on `forgot-password` as the first thing to add if public. No commit — user audits first. |
| 2026-07-07 | statements/frontend | D22 dashboard "delete": add a soft-archive trash action so users can declutter the dashboard, with the product requirement to retain data server-side. `statements.deleted_at` + migration `a7_statement_archive_001`; archive/restore/permanent endpoints; owner-facing statement + analysis queries filter archived; frontend trash icon + Undo flow; docs (DATA_MODEL §2–§4, DECISIONS D22, API.md, DESIGN.md §6) | 4 | Red zone: surfaced the conflict between "retain for the business" and the documented right-to-be-forgotten / data-minimization stance, and had the user decide before coding. Chose a two-tier design (reversible soft archive **plus** an explicit permanent hard delete) so retention is disclosed and true erasure still exists — not a silent soft-delete-only. `DELETE /{id}` semantics changed to archive; verified the old "DELETE then GET 404" contract still holds. Analysis visibility scoped via a join on `statements.deleted_at` (no flag duplicated onto children). UX: low-emphasis icon + Undo instead of a modal (none in the design system yet). Follow-up (same D22): added the `/dashboard/archived` view (`GET /api/statements?archived=true` + `deleted_at` in `StatementResponse`) with Restore + Delete-permanently (inline two-step confirm), and confirmed the design has **no auto-purge** (would contradict "retain for the business"). |

